package Journals;

import java.util.*;
import java.lang.reflect.Array;

public class matixboi {
	int[][] array = new int[6][6];
	Arrays.fill (array, (int) Math.random()*10);
	for (int i = 0; i < array.length; i++)
	{
		for (int z = 0; z < );)
	}
}
